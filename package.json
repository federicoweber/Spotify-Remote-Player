{
  "name": "spotify-album-sequencer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "One-page app to browse your Spotify albums and play them with a configurable gap between tracks, controlling a lossless-capable Connect device.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5005"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
